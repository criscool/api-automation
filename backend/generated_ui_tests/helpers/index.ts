/**
 * Helper 桶导出 - AI 生成的 .spec.ts 一行 import 拿到全部能力。
 *
 * 用法:
 *   import { loginAsDefault, searchInList, navigateToMenu, openRowByText, confirmDialog } from "../helpers";
 *
 * 不要在 helpers 内部互相 from "./index" 引,直接相对路径,避免循环依赖。
 */
export * from "./auth";
export * from "./common";
